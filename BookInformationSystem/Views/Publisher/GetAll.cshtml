@model IEnumerable<BookInformationSystem.Models.Domain.Publisher>
@{
}

<div class="container pt-5">
    <div class="row p-3">
        <div class="col-8 text-left">
            <h3>Publisher</h3>
        </div>
        <div class="col-4 text-right">
            <a class="btn btn-dark" href="/Publisher/Add">+ Add More</a>
        </div>
        <form asp-controller="Publisher" asp-action="GetAll">
            <p>
                <input type="text" name="SearchString" />
                <input type="submit" value="Search" />
            </p>
        </form>
    </div>
            <div class="table-responsive">
            <table class="table table-stripped table-bordered table-dark">
                <thead>
                    <tr>
                        <th>Publisher Name</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model)
                    {
                        <tr>
                            <td>@item.PublisherName</td>
                            <td>
                                <a class="btn btn-success" href="/Publisher/Update?id=@item.Id"><i class="bi bi-pen"></i></a>
                                <a class="btn btn-danger" onclick="return window.confirm('Are you sure?')" href="/Publisher/Delete?id=@item.Id"><i class="bi bi-trash"></i>

                        </a>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            </div>
        </div>
